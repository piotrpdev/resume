// eslint-disable-next-line no-unused-vars
import React from "react";

export default function HeadingTitle({ children }) {
  return (
    <span className="headingTitle">{children}</span>
  )
}
